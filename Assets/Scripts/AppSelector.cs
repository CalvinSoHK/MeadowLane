using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//Controls our app icons that helps us select and store the app this icon is associated with.
public class AppSelector : MonoBehaviour {

    //The app screen that needs to be spawned corresponding to this app
    public Transform APP_SCREEN;

    //In case the apps start taking a while to load, we'll show this transform down the line.
    //public Transform LOAD_SCREEN;

}
